# Working Prototype for Desktop

A working prototype for an app that could be used both on the desktop and mobile.

## Building

- clone the repo
- `cd max-proto`
- `npm i`
- `npm run build`
- `npx cap sync`
